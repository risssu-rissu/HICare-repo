version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb://mongo:27017/hicare
    depends_on:
      - mongo
    restart: always

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: always

  bridge:
    build:
      context: .
      dockerfile: Dockerfile.bridge
    environment:
      - API_URL=http://web:3000/api/ingest
      - NODE_OPTIONS=--dns-result-order=ipv4first
    depends_on:
      - web
    restart: always

volumes:
  mongodb_data:
